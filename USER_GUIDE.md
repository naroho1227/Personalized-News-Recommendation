# 사용자 가이드

## 목차

1. [서비스 소개](#1-서비스-소개)
2. [실행 전 준비사항](#2-실행-전-준비사항)
3. [서비스 실행 방법](#3-서비스-실행-방법)
4. [회원가입 방법](#4-회원가입-방법)
5. [로그인 방법](#5-로그인-방법)
6. [뉴스 추천받기](#6-뉴스-추천받기)
7. [기사 클릭 및 관심도 반영](#7-기사-클릭-및-관심도-반영)
8. [관심도 현황 확인](#8-관심도-현황-확인)
9. [오류 해결 방법](#9-오류-해결-방법)
10. [마무리](#10-마무리)

---

## 1. 서비스 소개

개인 맞춤형 뉴스 추천 플랫폼은 사용자의 관심 카테고리와 뉴스 클릭 기록을 바탕으로 개인에게 적합한 뉴스를 추천해주는 서비스이다.

사용자는 회원가입 후 로그인하여 추천 뉴스를 확인할 수 있으며, 뉴스 기사를 클릭하면 해당 카테고리의 관심도가 증가한다. 이후 사용자의 관심도 변화가 다음 뉴스 추천 결과에 반영된다.

주요 기능은 다음과 같다.

- 회원가입
- 로그인
- 관심 카테고리 선택
- 개인 맞춤형 뉴스 추천
- 뉴스 클릭 기록 저장
- 카테고리별 관심도 현황 확인

---

## 2. 실행 전 준비사항

서비스 실행을 위해 다음 환경이 필요하다.

- Python
- Docker 또는 Docker Compose
- PostgreSQL
- 웹 브라우저
- Git

프로젝트를 실행하기 전에 Python 패키지 설치와 데이터베이스 실행이 필요하다.

---

## 3. 서비스 실행 방법

프로젝트를 다운로드한 뒤 터미널에서 프로젝트 폴더로 이동한다.

```bash
git clone https://github.com/naroho1227/Personalized-News-Recommendation.git
cd Personalized-News-Recommendation
```

Docker를 사용하는 경우 `docker` 폴더로 이동하여 데이터베이스를 실행한다.

```bash
cd docker
docker-compose up -d
```

이후 백엔드 폴더로 이동하여 필요한 패키지를 설치한다.

```bash
cd ../backend
pip install -r requirements.txt
```

FastAPI 서버를 실행한다.

```bash
uvicorn app.main:app --reload
```

서버가 정상적으로 실행되면 웹 브라우저에서 아래 주소로 접속한다.

```text
http://localhost:8000
```

---

## 4. 회원가입 방법

사용자는 다음 순서로 회원가입을 진행한다.

1. 서비스 접속 후 `회원가입` 탭을 클릭한다.
2. 이메일을 입력한다.
3. 비밀번호를 입력한다.
4. 닉네임을 입력한다.
5. 관심 카테고리를 선택한다.
6. `가입하기` 버튼을 클릭한다.

회원가입 시 선택한 관심 카테고리는 초기 뉴스 추천 결과에 반영된다.

이미 사용 중인 이메일로 회원가입을 시도하면 가입이 제한될 수 있다.

---

## 5. 로그인 방법

사용자는 다음 순서로 로그인한다.

1. `로그인` 탭을 클릭한다.
2. 회원가입 때 사용한 이메일을 입력한다.
3. 비밀번호를 입력한다.
4. `로그인` 버튼을 클릭한다.
5. 입력한 정보가 올바르면 뉴스 추천 메인 화면으로 이동한다.

이메일 또는 비밀번호가 올바르지 않으면 로그인 오류 메시지가 표시된다.

---

## 6. 뉴스 추천받기

로그인 후 메인 화면에서 개인 맞춤형 뉴스를 추천받을 수 있다.

사용 방법은 다음과 같다.

1. 로그인 후 메인 화면으로 이동한다.
2. `새로 추천받기` 버튼을 클릭한다.
3. 사용자의 관심도 정보를 바탕으로 추천 뉴스 목록이 표시된다.
4. 추천 뉴스는 여러 카테고리의 기사로 구성된다.
5. 사용자의 클릭 기록이 쌓이면 추천 결과가 점점 개인화된다.

추천 뉴스는 RSS 피드를 통해 수집된 최신 뉴스 정보를 바탕으로 제공된다.

---

## 7. 기사 클릭 및 관심도 반영

추천 뉴스 목록에서 기사를 클릭하면 해당 행동이 사용자 기록에 반영된다.

사용 방법은 다음과 같다.

1. 추천 뉴스 목록에서 읽고 싶은 기사를 찾는다.
2. 해당 뉴스의 `기사 보기` 버튼을 클릭한다.
3. 클릭한 뉴스의 카테고리가 사용자 기록에 저장된다.
4. 해당 카테고리의 관심도 점수가 증가한다.
5. 이후 뉴스 추천 결과에 클릭 기록이 반영된다.

예를 들어 기술 카테고리 뉴스를 자주 클릭하면 기술 분야 뉴스가 더 많이 추천될 수 있다.

다만 특정 카테고리만 지나치게 추천되지 않도록 다른 카테고리 뉴스도 일정 비율 이상 추천되도록 설계되어 있다.

---

## 8. 관심도 현황 확인

메인 화면의 관심도 현황 영역에서 카테고리별 관심도 비율을 확인할 수 있다.

관심도 현황은 사용자의 뉴스 클릭 기록을 바탕으로 변경된다.

확인 방법은 다음과 같다.

1. 로그인 후 메인 화면으로 이동한다.
2. 오른쪽 관심도 현황 영역을 확인한다.
3. 뉴스 기사를 클릭한다.
4. 클릭한 카테고리의 관심도 비율이 변화하는지 확인한다.
5. 다시 추천받기를 누르면 변경된 관심도가 추천 결과에 반영된다.

관심도 현황을 통해 사용자는 자신의 뉴스 관심 분야가 어떻게 변화하는지 확인할 수 있다.

---

## 9. 오류 해결 방법

| 문제 상황 | 원인 | 해결 방법 |
|---|---|---|
| 서비스 화면이 열리지 않음 | 서버가 실행되지 않음 | FastAPI 서버가 실행 중인지 확인한다. |
| 데이터베이스 연결 오류가 발생함 | PostgreSQL이 실행되지 않음 | Docker 컨테이너 또는 PostgreSQL 실행 상태를 확인한다. |
| 로그인이 되지 않음 | 이메일 또는 비밀번호 오류 | 입력한 이메일과 비밀번호를 다시 확인한다. |
| 회원가입이 되지 않음 | 이미 가입된 이메일 사용 | 다른 이메일을 사용하거나 기존 계정으로 로그인한다. |
| 뉴스가 표시되지 않음 | 추천 버튼을 누르지 않았거나 RSS 수집 문제가 발생함 | `새로 추천받기` 버튼을 누르고 잠시 기다린다. |
| 관심도 변화가 보이지 않음 | 기사를 클릭하지 않음 | 추천 기사에서 `기사 보기` 버튼을 클릭한다. |
| 서버 실행 중 오류가 발생함 | 필요한 패키지가 설치되지 않음 | `pip install -r requirements.txt` 명령어를 다시 실행한다. |

---

## 10. 마무리

본 사용자 가이드는 개인 맞춤형 뉴스 추천 플랫폼의 기본 사용 방법을 설명한다.

사용자는 회원가입과 로그인을 통해 서비스를 이용할 수 있으며, 추천 뉴스를 클릭하면 관심도 정보가 반영되어 이후 추천 결과가 개인화된다.

이를 통해 사용자는 자신이 관심 있는 분야의 뉴스를 더 빠르게 확인할 수 있다.
